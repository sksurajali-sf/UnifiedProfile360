<!--
  See dcProfileHeader for why each panel has its own shell.
-->
<aura:component
    implements="flexipage:availableForAllPageTypes,force:hasRecordId,force:hasSObjectName"
    access="global"
    description="Pick any data model object linked to this individual and page through its records."
>
    <aura:attribute name="recordId" type="String" access="global" />
    <aura:attribute name="sObjectName" type="String" access="global" />

    <aura:attribute name="pageSize" type="Integer" default="5" access="global" />

    <c:unifiedDataExplorer
        recordId="{!v.recordId}"
        objectApiName="{!v.sObjectName}"
        pageSize="{!v.pageSize}"
    />
</aura:component>
